% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eORA.R
\name{snps_to_ghid}
\alias{snps_to_ghid}
\title{Map SNPs (rsIDs and/or genomic loci) to GeneHancer IDs using a BED file path}
\usage{
snps_to_ghid(
  x,
  gh_bed,
  ghid_col = 4L,
  input_type = c("auto", "rsid", "loc_string", "dataframe"),
  loc_regex = "^(chr)?([0-9]+|X|Y|MT|M)[:_](\\\\d+)(?:-(\\\\d+))?$",
  include_sex_chr = FALSE,
  chr_style = c("chr", "nochr"),
  ensembl_version = NULL,
  grch = 38,
  return = c("ghid", "mapping", "by_input"),
  unique_ghid = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{x}{Character vector of rsIDs and/or locus strings, OR a data.frame with either:
(1) chr + pos columns, or (2) chr + start + end columns (case-insensitive).}

\item{gh_bed}{File path to a GeneHancer BED file with no header.}

\item{ghid_col}{Integer column index in the BED containing GHID/GeneID. Default 4.}

\item{input_type}{One of "auto", "rsid", "loc_string", "dataframe".}

\item{loc_regex}{Regex to parse locus strings.}

\item{include_sex_chr}{If FALSE (default), keep autosomes only. If TRUE, include X/Y/MT.}

\item{chr_style}{"chr" (default) uses "chr1" style; "nochr" uses "1" style.}

\item{ensembl_version}{Optional integer Ensembl version to pin SNP mapping (unused in useMart mode).}

\item{grch}{Genome reference (kept for API compatibility; not used in useMart mode).}

\item{return}{One of "ghid", "mapping", "by_input".}

\item{unique_ghid}{If TRUE (default) and return="ghid", return unique GHIDs.}

\item{quiet}{Suppress informational messages.}
}
\value{
Depending on return:
  - "ghid": character vector of GHIDs
  - "mapping": data.frame(INPUT, GHID)
  - "by_input": named list input -> GHID vector
}
\description{
GeneHancer BED is assumed to be BED0 (0-based start, end-exclusive).
Internally converted to 1-based inclusive coordinates for GenomicRanges.
}
