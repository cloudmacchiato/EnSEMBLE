% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssEA.R
\name{select_enhancer_set_helpers}
\alias{select_enhancer_set_helpers}
\title{Select High-Confidence Enhancer-Set Helpers from ESEA Results}
\usage{
select_enhancer_set_helpers(
  esea_result,
  qcut = 0.05,
  use_elbow = TRUE,
  use_connect = TRUE,
  k_mad = 0,
  qcol = "qValue",
  id_col = "Compare.List",
  le_col = "leadingEdge"
)
}
\arguments{
\item{esea_result}{Data frame with ESEA output. Must include the columns specified
in \code{qcol}, \code{id_col}, and \code{le_col}.}

\item{qcut}{Numeric. Maximum q-value to retain (default \code{0.05}).}

\item{use_elbow}{Logical. If \code{TRUE}, retain only helpers up to the elbow point
of the ordered \code{qcol} values (default \code{TRUE}).}

\item{use_connect}{Logical. If \code{TRUE}, compute cosine-based connectivity among
leading-edge sets and filter by \code{k_mad} (default \code{TRUE}).}

\item{k_mad}{Numeric multiplier on the MAD of connectivity scores that defines the
minimum connectivity to retain (default \code{0}).}

\item{qcol}{Character. Column name storing q-values (default \code{"qValue"}).}

\item{id_col}{Character. Column holding enhancer-set identifiers
(default \code{"Compare.List"}).}

\item{le_col}{Character. Column with comma-separated leading-edge enhancers
(default \code{"leadingEdge"}).}
}
\value{
A filtered data frame ordered by \code{qcol} (and connectivity when applicable)
  and containing a \code{connectivity} column summarising helper similarity.
}
\description{
Filters ESEA output to retain helper enhancer sets that satisfy a q-value cutoff,
an optional elbow heuristic, and an optional connectivity requirement derived from
leading-edge overlaps.
}
